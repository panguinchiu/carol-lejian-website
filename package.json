{
  "name": "carol-lejian-website",
  "version": "1.0.0",
  "private": true,
  "description": "樂見｜內在探索空間 — Carol's QHHT practice site",
  "scripts": {
    "build": "node build.js",
    "serve": "cd dist && python3 -m http.server 8080",
    "dev": "node build.js && cd dist && python3 -m http.server 8080"
  },
  "dependencies": {
    "handlebars": "^4.7.8"
  }
}
